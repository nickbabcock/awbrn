# AWBRN

[mise](https://mise.jdx.dev/) required to setup dev environment.

```cmd
git submodule init
git submodule update
mise run assets
mise run desktop
```

Drop replay onto window (eg: `assets/replays`)
