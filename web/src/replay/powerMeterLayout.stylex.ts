import * as stylex from "@stylexjs/stylex";

/** Fixed geometry for the CO power meter. */
export const powerMeterLayout = stylex.defineConsts({
  breakdownMinInlineSize: "132px",
  copStarSize: "13px",
  pendingInlineSize: "112px",
  scopStarSize: "19px",
});
