export const TWO_COLUMN_GRID_MIN_WIDTH = 320;

/** Pixel-backed sizes required by sprite and portrait component APIs. */
export const ROSTER_MEDIA_SIZE = {
  crest: 24,
  portrait: 36,
} as const;

/** Pixel-backed design tokens required by the match report's sprite component APIs. */
export const MATCH_REPORT_MEDIA_SIZE = {
  crest: 20,
  portrait: 40,
} as const;
